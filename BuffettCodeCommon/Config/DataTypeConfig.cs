namespace BuffettCodeCommon.Config
{
    // to use type => client.func
    public enum DataTypeConfig
    {
        Quarter,
        Daily,
        Monthly,
        Company,
        // only v2 (deprecated)
        Indicator,
    }
}